"""Scenario: classic 1-delete / 1-add replace block with shared prefix/suffix.

Comment on the deleted side OR the added side (sidebar shows one line).
"""


def greet(name):
    message = "Welcome, " + name + "!"
    return message
